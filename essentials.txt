

CREATE KEYSPACE user WITH REPLICATION = {'class' : 'Initial', 'replication_factor' : 1};

CREATE TABLE user.namesANDpasswords 
(USERNAME text, PASSWORD text PRIMARY KEY(NAME, PASSWORD));

INSERT INTO user.nameANDpassword(USERNAME, PASSWORD) VALUES('sultan', '24e2a20dcb8d414d13a21209ad26624e');

FROM python:3.7 − alpine
WORKDIR /Goodbooks
COPY . /Goodbooks
RUN pip install −U −r requirements.txt
EXPOSE 8080
CMD [" python" , "Goodbooks.py"]



